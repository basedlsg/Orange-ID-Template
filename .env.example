# SQLite is the only supported database
# No database configuration needed - it works automatically!

# Optional: Custom session secret (set this to something unique in production)
SESSION_SECRET=orange-auth-template-secret

# Debug settings (uncomment to enable SQLite debugging)
# DEBUG_SQLITE=true
